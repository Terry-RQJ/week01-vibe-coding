# PRD.md — 法律小科普轻站 V1

> 第 1 周·Day 4 产品需求文档  
> 本文档基于 `research.md`（Day 3）编写；Day 5 才做技术选型；Day 7 据此出 MVP。  
> **第 8 节「验收标准」是 Day 7 上线的检查清单**，上线当天逐条打勾。

---

## 0. 元信息

| 字段 | 值 |
|------|----|
| 产品名 | 法律小科普轻站 |
| 项目代号 | week01-vibe-coding（Vibe Coding 学习项目）|
| 文档版本 | v1.1（Day 4 重写补全版） |
| 文档日期 | 2026-09-18 |
| 上游文档 | `research.md`（Day 3）|
| 下游文档 | 技术选型（Day 5）、UI 设计（Day 6）、MVP 上线（Day 7）|
| 目标周期 | Day 1 ~ Day 7 = V1 MVP；Day 8 ~ Day 28 = 内容填充与优化 |

---

## 1. 产品概述

### 1.1 一句话定位
**让一个普通人在「撞上事」的瞬间，能 3 分钟搞懂「这事归哪条法律管、我现在该怎么做」。**

### 1.2 解决什么
- **看不懂**：法条术语太专业，普通人读 10 分钟还是不知道「这跟我有什么关系」
- **找不到**：搜索引擎广告太多，法律问答内容真假混杂
- **用不上**：就算读到一段科普，也不清楚「我现在能做什么」
- **易害怕**：很多人以为自己「摊上事了」，其实只是日常摩擦，但没人告诉他这不是法律问题

### 1.3 产品形态
- **类型**：纯静态内容站（V1 不做账号、不做对话 AI）
- **风格**：卡片化结构化知识，每张卡片对应一个**真实生活情境**
- **核心动作**：浏览「情形清单」→ 打开一个情境 → 看完整套路（情形 + 法条 + 怎么解决 + 看情况选我做 → 怎么做 + 还能扩展了解）

### 1.4 V1 不解决什么
- 不做具体法律咨询（这是律师的工作，参见 §9.1 合规边界）
- 不做案件胜诉率预测
- 不替代 12348 法律服务热线 / 法律援助
- 不覆盖所有法律领域（V1 只覆盖 §3 列出的 6 大情形）

---

## 2. 目标用户

### 2.1 用户画像

| 维度 | 描述 |
|------|------|
| 年龄 | 22 ~ 55 岁 |
| 身份 | 普通上班族 / 大学生 / 个体工商户 / 小企业主 / 退休 / 家庭主妇 |
| 法律知识 | 基本为零，遇到事才想起来查 |
| 设备 | 80% 手机端访问，常用微信扫一扫 / 朋友转发 |
| 行为特征 | 「时间紧 + 焦虑 + 怕被骗」，要看一眼就懂的科普；不愿下载 App、不愿注册 |
| 痛点 | 看完一段科普还是不知道「我现在该打电话给谁、要不要签字」 |

### 2.2 典型使用场景

| # | 用户处境 | 用户疑问 | 进站后路径 |
|---|---------|---------|-----------|
| U1 | 收到公司「协商解除合同」通知 | 我能要求赔偿吗？要签字吗？ | 首页 → 劳动分类 → 「被公司劝退」卡 → F3 选你的合同类型 → 看到「不建议签字」+ 法条 |
| U2 | 租房到期房东不退押金 | 我能去仲裁吗？要花多少钱？ | 首页 → 借贷/合同分类（实际归类到住房）→「房东不退押金」卡 → F3 选是否搬离 → 看仲裁流程 |
| U3 | 微信群里被骂，气得要起诉 | 我能告他吗？要花多少钱？ | 首页 → 邻里/名誉分类 → 「被人在网上骂」卡 → F3 选传播范围 → 看「相关拓展」维权的 3 种路径 |
| U4 | 逛超市买到过期食品 | 我能要求 1000 元赔偿吗？ | 首页 → 消费分类 → 「买到假货 / 过期食品」卡 → 直接看《消费者权益保护法》第 55 条 |
| U5 | 朋友找我借钱，要不要写借条 | 借条怎么写才有法律效力？ | 首页 → 借贷分类 → 「朋友借钱」卡 → 看「借条范本」和 4 个雷区 |
| U6 | 走路被车撞，对方说没保险 | 我能怎么办？要不要请律师？ | 首页 → 交通分类 → 「交通事故赔偿」卡 → F3 判断树 → 看「是否报警」「代位求偿」怎么走 |

---

## 3. MVP 功能

V1 共 **7 个功能**，按「用了就有，没有就难受」的优先级划分。

### 3.1 情形清单首页（F1）
- **是什么**：首页是个**真实生活情境的清单**，用户进来看到「被公司劝退」「房东不退押金」「买到过期食品」这些标题
- **为什么是清单不是搜索框**：用户不是来搜索的，是来「看一眼这事归不归我」的；清单形式让他 3 秒判断要不要继续看
- **情形分类**（V1 至少覆盖 6 类）：
  1. **劳动类**：被劝退 / 加班费 / 工伤认定 / 试用期辞退
  2. **消费类**：过期食品 / 假货 / 三包 / 套路贷 / 大件退换
  3. **借贷类**：朋友借钱 / 借条怎么写 / 民间借贷利息 / 信用卡纠纷
  4. **婚姻家庭类**：婚前财产 / 离婚冷静期 / 抚养费 / 继承
  5. **交通类**：交通事故赔偿 / 代位求偿 / 网约车纠纷 / 酒驾同乘责任
  6. **邻里 / 名誉类**：噪音扰民 / 高空抛物 / 网络名誉侵权 / 群聊骂人
- **用户动作**：进入首页 → 看到 N 张卡片（按情形分组）→ 可按情形标签过滤 → 点击任一卡片进入详情
- **可观察结果**：
  - 页面加载完成 < 2 秒（4G 网络）
  - **至少 12 张卡片**（6 类 × 各 2 张）已发布
  - 每张卡片显示：标题、一句话摘要（≤ 60 字）、所属分类标签、发布日期（粗略到月）

### 3.2 详情页：单张情境卡（F2）
- **是什么**：打开一张卡片后看到的完整内容
- **5 个固定模块**（顺序固定，缺一不可）：
  1. **情形**：场景化语言说「这事是这样的」（≤ 150 字，含「你是张三/李四」式代入）
  2. **法律条文**：列 1 ~ 3 条相关法条原文（每条带可点开的出处链接）
  3. **怎么解决**：3 步内的「先这样 → 再这样 → 再这样」行动指南（每步含具体动作：电话、签字、拍照、留证）
  4. **看情况选我做**（F3 入口）：跳到 §3.3 个人情况说明
  5. **相关拓展**：3 ~ 5 个「相关情形」卡片链接，去看更多
- **用户动作**：滚动浏览各模块；点法条出处链跳转源网站；点「相关拓展」去别的卡片
- **可观察结果**：
  - 整页内容 **3 分钟内读完**（按 250 字/分钟正常人阅读速度）
  - 每个法条都有**可点开的原文链接**（不是「参考某某法」这种空话）
  - 页脚固定显示「免责声明 + 内容生产流程 + 反馈入口」

### 3.3 个人情况说明选项：看情况选我做（F3）
- **是什么**：一个**互动决策树**，让用户告诉他「我属于哪一种具体情形」，系统给出**对应的下一步动作**
- **关键：不是 AI 对话**：这是**预制的选项树**，每张卡 3 ~ 5 个分支，问题 ≤ 3 层，所有路径纯前端写死
- **示例**：「被公司劝退」卡片里的决策树
  - 第 1 问：你的劳动合同签了吗？
    - ① 没签 → 终点：「事实劳动关系」，可以主张双倍工资 + 离职赔偿
    - ② 签了 → 第 2 问：合同到期了吗？
      - ① 到期 → 终点：公司不续签需支付经济补偿金 N 倍
      - ② 没到期 → 第 3 问：是公司劝退还是你主动想走？
        - ① 公司劝退 → 终点：建议不签字 + 录音留证 + 主张赔偿 2N
        - ② 自己想走 → 终点：可主张经济补偿金 N
- **用户动作**：点开「看情况选我做」→ 看问题 1 → 选某项 → 根据选择，下一步问题/结论变化 → 到达终点看到「建议你这样做」+ 法条依据
- **可观察结果**：
  - 每张卡**至少 3 个分支终点**，每个终点都有法条 + 行动
  - 用户选择都**纯前端处理**（不发送任何数据，符合 §9 合规）
  - 顶部分支路径清晰显示「第几步 / 共几步」
  - 用户可以随时点「返回上一步」或「重新选」

### 3.4 收藏 / 备注（F4，详情页内）
- **是什么**：用户想把某张卡片**保存下来**以后再看的入口
- **必须本地**：V1 不做账号，所以存到**浏览器 localStorage**，用户清除浏览器数据 = 丢失（必须明示）
- **用户动作**：
  - 在详情页点「收藏」按钮 → 状态变为「已收藏」
  - 在详情页点「写备注」→ 出现文本框 → 输入 → 保存（自动保存，≤ 500 字）
  - 在首页顶部「我的收藏」入口 → 看到自己收藏过的卡片列表
- **可观察结果**：
  - 「我的收藏」页面**只显示当前浏览器收藏过的卡片**，空时显示「还没有收藏，去首页看看吧」
  - 备注能保存最多 **500 字**，刷新页面不丢
  - 顶部有「当前共收藏 X 张」的提示
  - 详情页底部有「导出我的收藏」按钮（导出 JSON 文件，存到本地）

### 3.5 法律条文聚合页（F5）
- **是什么**：把详情页里出现的所有法条集中成一页，方便用户按法条反向查「哪张卡片提到了这条」
- **为什么有**：用户有时候是从外面看到「民法典第 X 条」来的，需要直接定位
- **用户动作**：在首页底部 / 关于页点「所有法条」入口 → 进入法条列表 → 按法律分类（《民法典》/《劳动法》/《消费者权益保护法》...）分组 → 点任一法条 → 看到原文 + 「被以下卡片引用」列表
- **可观察结果**：
  - 法条分类**至少覆盖 4 部法律**（《民法典》《劳动法》《消费者权益保护法》《道路交通安全法》）
  - 点法条项能看到原文 + 引用该法条的卡片数
  - 按发布日期排序（最新更新优先）

### 3.6 搜索 / 找卡（F6，轻量级）
- **是什么**：一个**纯前端关键词搜索**，搜标题和标签的简单匹配（不是复杂推荐算法）
- **后续功能**：复杂推荐留到 V2（参见 §4.1）
- **用户动作**：在首页顶部搜索框输入文字 → 实时过滤卡片列表
- **可观察结果**：
  - 输入「押金」能找到「房东不退押金」「租房押金纠纷」类卡片
  - 搜索**纯前端**完成（不发送任何用户输入给后端，符合 §9 合规）
  - 0 命中时显示「没找到，试试搜『借条』或『合同』」

### 3.7 关于页（F7，含免责声明）
- **是什么**：说明「我们是谁 / 怎么生产内容 / 不能替代律师 / 紧急情况找谁」
- **为什么是 P0**：法律内容站**没有免责声明 = 违法**（参见 §9.1 合规边界）
- **用户动作**：从首页底部 / 任意详情页底部进入
- **可观察结果**：
  - 完整写明：内容仅供学习参考、不构成法律咨询、紧急情况拨打 **12348 法律服务热线** 或 **110**
  - 「内容生产流程」说明每张卡片都经人工复核
  - 「反馈入口」邮箱 / 微信二维码 / 反馈表单

### 3.8 用户动作 → 可观察结果 速查表

| 功能 | 用户动作 | 可观察结果 |
|------|---------|----------|
| F1 情形清单 | 进入首页 | < 2 秒加载；至少 12 张卡；按分类显示 |
| F2 详情页 | 点击卡片 | 看到 5 个模块；3 分钟内读完；法条可点开 |
| F3 看情况选我做 | 选选项 | 路径清晰；3 层内给结论；纯前端 |
| F4 收藏 / 备注 | 点收藏 / 输入备注 | localStorage 保存；「我的收藏」页能找到 |
| F5 法条聚合 | 点「所有法条」 | 按 4+ 部法律分组；显示引用卡片数 |
| F6 搜索 | 输入关键词 | 实时过滤；纯前端；空结果显示提示 |
| F7 关于页 | 任何页面底部进入 | 免责声明可见；紧急联系方式可见 |

---

## 4. 后续功能（V2+，不在 Day 7 MVP 范围内）

按你的要求，以下功能**列为后续**，V1 不做：

### 4.1 用户登录 / 账号系统
- **为什么不做**：个保法备案 / 数据持久化 / 跨设备同步等都需要后端 + 法务审核，Day 7 内做不完；V1 用 localStorage 兜底已够用
- **V2 设想**：微信扫码登录、手机号 + 验证码、收藏跨设备同步

### 4.2 复杂推荐算法
- **为什么不做**：V1 用户量小、内容少，简单搜索已够；过早做推荐会引入「算法黑盒 + 内容放大」，法律领域风险高
- **V2 设想**：基于浏览历史的相似卡片推荐、热门卡片置顶、相关法条推荐

### 4.3 付费功能 / 会员
- **为什么不做**：V1 内容基本免费，付费反而把真正需要的人挡在门外；且付费功能涉及工商 / 税务 / 平台资质审核
- **V2 设想**：深度专题合集付费、专家问答付费、专业律师咨询入口（合规前提下）

### 4.4 V2+ 还可能加（不承诺）
- 用户案例投稿（需审核）
- 视频讲解（每张卡片 30 秒视频）
- 多语言版本（英语 / 粤语）
- 微信小程序版
- 邮件订阅更新（每周一份「本月值得关注的新情形」）

---

## 5. 页面与交互要求

### 5.1 页面 / 视图列表

V1 共 **6 个核心页面** + **3 个次级视图**：

| ID | 名称 | URL 路径 | 来源入口 | 备注 |
|----|------|---------|---------|------|
| **P1** | 情形清单首页 | `/` | 直接访问 / 任何页面底部「回到首页」 | 6 类情形 × 各 2+ 卡 |
| **P2** | 详情页 | `/article/:slug` | F1 点卡片 / F3 终点「相关拓展」 | 5 模块顺序固定 |
| **P3** | 法条聚合 | `/laws` | 首页底部「所有法条」链接 | 按法律分类 |
| **P4** | 我的收藏 | `/bookmarks` | 首页右上角图标 / 收藏按钮 | localStorage 数据源 |
| **P5** | 关于页 | `/about` | 任何页脚「关于」 | 含免责声明 |
| **P6** | 搜索结果 | `/?q=...`（也可独立页） | F6 搜索 | 纯前端过滤 |

**次级视图**：
| ID | 名称 | 出现位置 | 行为 |
|----|------|---------|------|
| V1 | 决策树组件 | F2 详情页内「看情况选我做」 | 模态层 / 内嵌 |
| V2 | 备注编辑 | F2 详情页内「写备注」 | 内嵌文本框 + 自动保存提示 |
| V3 | 404 兜底 | 任何未知 URL | 友好提示 + 「回到首页」按钮 |

### 5.2 交互规范

| 维度 | 要求 |
|------|------|
| 加载速度 | 首屏 < 2 秒（4G）；跳转 < 1 秒 |
| 字体 | 正文用思源黑体 / 系统默认无衬线；法条用等宽字体（便于阅读条款编号） |
| 字号 | 正文 16px 起；标题 ≤ 24px；移动端行高 ≥ 1.6 |
| 颜色 | 主色：稳重蓝（#1E40AF）；辅色：暖橙（#EA580C，用于警示句「不建议签字」之类）；背景：浅灰（#F9FAFB） |
| 响应式断点 | 375px / 768px / 1024px；手机端单列、平板/桌面双列 |
| 移动端 | 无横向滚动条；可点区域 ≥ 44×44px（手指友好） |
| 无障碍 | 文字对比度 ≥ 4.5:1；所有图片有 alt；键盘可达 |
| 反馈 | 收藏成功：「已收藏 ✓」提示，3 秒后消失；备注保存：「已自动保存」 |

---

## 6. 数据字段

### 6.1 内容数据（卡片）

每张卡片（文章）至少包含以下字段：

| 字段名 | 类型 | 是否必填 | 说明 |
|--------|------|---------|------|
| `id` | string (UUID) | ✅ | 全局唯一 |
| `slug` | string (kebab-case) | ✅ | URL 用，例如 `qiye-cuidui` |
| `title` | string | ✅ | 卡片标题，≤ 30 字 |
| `summary` | string | ✅ | 一句话摘要，≤ 60 字，首页显示 |
| `category` | enum | ✅ | 所属分类：劳动 / 消费 / 借贷 / 婚姻家庭 / 交通 / 邻里 |
| `tags` | string[] | ✅ | 标签列表，例：`["公司劝退", "赔偿", "录音"]` |
| `scenario` | string | ✅ | 「情形」模块，≤ 150 字 |
| `laws` | LawRef[] | ✅ | 「法律条文」模块，每条 ≥ 1 条 |
| `solution_steps` | string[] | ✅ | 「怎么解决」模块，3 步内 |
| `decision_tree` | DecisionNode | ✅ | 「看情况选我做」决策树，根节点 |
| `related_articles` | string[]（id 列表）| ✅ | 「相关拓展」卡片 ID，3 ~ 5 个 |
| `published_at` | ISO date | ✅ | 发布日期 |
| `updated_at` | ISO date | ✅ | 最后更新日期 |
| `author_type` | enum | ✅ | 「AI 起草 + 律师复核」/ 「全部律师手写」 |
| `reviewed_by` | string | ✅ | 复核人姓名 + 资质（V1 占位即可） |
| `status` | enum | ✅ | `draft` / `published` / `archived` |

### 6.2 法条引用（LawRef）

| 字段名 | 类型 | 是否必填 | 说明 |
|--------|------|---------|------|
| `law_name` | string | ✅ | 法律名，如「中华人民共和国民法典」 |
| `article_no` | string | ✅ | 条款号，如「第 1062 条」 |
| `content` | string | ✅ | 法条原文（≤ 200 字） |
| `source_url` | URL | ✅ | 可点开的出处链接（国家法律法规数据库 / 北大法宝 / 政府官网） |
| `effective_date` | ISO date | 否 | 生效日期，便于以后版本切换 |

### 6.3 决策树节点（DecisionNode）

```yaml
node:
  id: string              # 节点 ID
  type: question | result # question=选择题, result=终点
  text: string            # 题目或结论
  options:                # type=question 时有
    - label: string       # 选项文案
      next: DecisionNode  # 指向下个节点（result 时为空）
  law_refs: LawRef[]      # type=result 时列出本结论对应的法条
  action: string          # type=result 时的「建议你这样做」
```

### 6.4 用户收藏 / 备注（本地存储）

存于 `localStorage`，**不发送到任何服务器**：

| Key | 类型 | 说明 |
|-----|------|------|
| `bookmarks` | string[]（卡片 ID）| 已收藏卡片 ID 列表 |
| `notes:<article_id>` | string | 每张卡片一条备注（≤ 500 字）|
| `bookmarks_updated_at` | ISO date | 最后修改时间（用于「导出收藏」） |

### 6.5 法条聚合索引（前端计算）

在 build 时 / 运行时从卡片数据里聚合：

| 字段 | 来源 |
|------|------|
| `law_name` | LawRef.law_name |
| `article_no` | LawRef.article_no |
| `source_url` | LawRef.source_url |
| `referenced_by` | 反向索引：所有引用此法条的卡片 ID |

---

## 7. 异常状态

| 状态码 | 触发场景 | 应展示页面 / 行为 |
|--------|---------|------------------|
| **404** | URL 不存在 / 卡片已下架 | 友好提示页：「这篇卡片暂时无法显示，去首页看看吧」+ 返回首页按钮 |
| **500** | 静态资源加载失败（理论上极少） | 「页面加载出错了，请刷新」+ 联系我们邮箱 |
| **空数据 1** | 「我的收藏」无任何条目 | 「还没有收藏，去首页看看吧」+ 跳转链接 |
| **空数据 2** | 搜索 0 命中 | 「没找到匹配的内容，试试搜『借条』或『合同』」 |
| **空数据 3** | 「相关拓展」找不到相关 ID | 显示「更多内容正在准备中」 |
| **断网** | 用户离线访问 | 浏览器原生断网页面（静态站天然支持）|
| **localStorage 不可用** | 用户禁用本地存储 | 收藏按钮 hover 提示「需允许浏览器存储才能使用收藏」 |
| **法条链接失效** | 出处 URL 失效 | 该法条链接点开前显示「此链接可能已失效，请以法律原文为准」（V1 不做自动检测，提示即可）|
| **JS 禁用** | 用户禁用 JS | P1 / P5 / P6 仍能显示静态文本（卡片纯文本版），但 F3 / F4 / F6 不可用并提示 |

---

## 8. 验收标准（可逐条检查）

Day 7 上线当天按下列清单**逐条打勾**，全部 ✅ 才算 MVP 完成。

### 8.1 功能验收（F1 ~ F7）

- [ ] **F1**：首页 6 大类情形都至少有 2 张已发布卡片（共 ≥ 12 张）
- [ ] **F1**：首页按分类显示卡片，每张卡片有标题 + 一句话摘要 + 分类标签 + 发布月份
- [ ] **F1**：首页加载时间（4G 模拟）< 2 秒
- [ ] **F2**：点任一卡片进入详情，看到 5 个模块（情形 / 法条 / 怎么解决 / 看情况选我做 / 相关拓展）顺序固定
- [ ] **F2**：详情页所有内容 < 3 分钟读完（按 250 字/分钟计）
- [ ] **F2**：每张卡片至少引用 1 条法条，每条法条都有可点开的原文链接（链接能打开到对应页面）
- [ ] **F3**：至少 3 张卡片实现了「看情况选我做」决策树
- [ ] **F3**：决策树深度 ≤ 3 层，每张卡至少 3 个分支终点
- [ ] **F3**：所有决策树路径**纯前端处理**（检查 Network 面板，无任何请求）
- [ ] **F4**：详情页有「收藏」按钮，点击后状态变为「已收藏 ✓」
- [ ] **F4**：详情页有「写备注」入口，输入文字后自动保存，刷新页面备注仍在
- [ ] **F4**：首页有「我的收藏」入口，进入后**只显示当前浏览器收藏过的卡片**
- [ ] **F4**：备注最多 500 字（超出截断并提示）
- [ ] **F4**：顶部有「当前共收藏 X 张」计数
- [ ] **F5**：「所有法条」入口在首页底部和关于页都能找到
- [ ] **F5**：法条按 4 部以上法律（《民法典》/《劳动法》/《消费者权益保护法》/《道路交通安全法》）分组
- [ ] **F5**：点任一法条能看到原文 + 引用该法条的卡片列表
- [ ] **F6**：首页有搜索框，输入关键词实时过滤卡片
- [ ] **F6**：搜索**纯前端**（Network 面板无请求）
- [ ] **F6**：0 命中时显示「没找到，试试搜『借条』或『合同』」提示
- [ ] **F7**：关于页能从任意页脚进入
- [ ] **F7**：包含完整免责声明（不构成法律咨询）+ 紧急联系方式（12348 / 110）
- [ ] **F7**：包含「内容生产流程」和「反馈入口」

### 8.2 内容验收

- [ ] 每张卡片的「情形」开头用「假设你是…」「假设张三…」「假设你 … 了」句式代入
- [ ] 每张卡片的法条原文**逐字引用**，不改编（改编算侵权）
- [ ] 每张卡片的「怎么解决」步骤**具体到动作**（「打电话给 12348」「拍 3 张照片：合同 / 收据 / 商品」），不写空话
- [ ] 每张卡片都标了 `author_type`（AI 起草 + 律师复核 / 全部律师手写）
- [ ] `reviewed_by` 字段 V1 占位（无需真律师签字）但**必须有此字段**
- [ ] 所有法条链接**点开能打开到正确页面**（404 / 302 重定向到无关页 = 不合格）
- [ ] 无明显错别字 / 排版错误（人工抽查 3 张卡片）

### 8.3 合规验收（最关键，U-04 最高优先级）

- [ ] **关于页**含完整免责声明：内容仅供学习参考、不构成法律咨询
- [ ] **关于页**含紧急联系方式：12348 法律服务热线 / 110 / 司法局法律援助
- [ ] **关于页**明确说明「本站不提供个案咨询，个案请咨询执业律师」
- [ ] **详情页页脚**每页都有「本页内容不构成法律咨询」字样
- [ ] **不收取任何费用**（无付费入口、无广告联盟代码、Check-out 流程为零）
- [ ] **不收集任何用户身份信息**（无注册、无表单需要填手机号 / 身份证号）
- [ ] **收藏 / 备注**明确说明存于 localStorage（用户清除浏览器数据 = 丢失）
- [ ] **决策树**不直接输出「你应该去告 / 不告」等结论（只输出「建议这样做」+ 法条依据）
- [ ] 所有法条链接指向**官方源**（国家法律法规数据库 / 全国人大官网 / 政府部门官网），不引私人博客

### 8.4 部署 & 可访问性验收

- [ ] 网站能通过公开 URL（GitHub Pages / Vercel / Netlify）访问，**HTTPS 启用**
- [ ] 首页在 4G 模拟下加载 < 2 秒（Chrome DevTools 测试）
- [ ] 在 375px / 768px / 1024px 三个宽度下都能正确显示，**无横向滚动条**
- [ ] 移动端所有可点区域 ≥ 44×44px
- [ ] 文字与背景对比度 ≥ 4.5:1（Chrome Lighthouse 跑 ≥ 90）
- [ ] 键盘可达：Tab 键能顺序走过所有交互元素
- [ ] 所有图片有 `alt` 文案
- [ ] Chrome / Safari / 微信内置浏览器 / Edge 最新版都能正常打开

### 8.5 Git & 版本验收

- [ ] V1 全部源码在 GitHub 仓库 `Terry-RQJ/week01-vibe-coding` 公开
- [ ] **没有 .env 等敏感文件**（API 检查 `/contents/.env` 返回 404）
- [ ] 所有 Token / 密钥在部署平台以加密方式存储，不进 repo
- [ ] `research.md` / `PRD.md` / `index.html` 都在仓库可访问
- [ ] 提交记录标题以「Day N｜」开头，可追溯每天进度

---

## 9. 合规与免责声明

### 9.1 合规边界（V1 致命问题）

根据 `research.md` 第 9 节不确定项 **U-04**（非律师提供法律意见的合规边界）：

- 《律师法》第 14 条规定：没有律师执业证书的人**不得以律师名义从事法律服务业务**
- V1 风险点：决策树的结论「建议你这样做」是否构成「提供法律意见」？业内通行做法是**只输出法条依据 + 通用行动指南，不下结论**
- V1 应对：所有决策树结论用「建议先…再…」「可考虑…」的句式，避免「你应该起诉」「你不应该签」等判断
- 详细法律渊源：参见 `research.md` 第 8 节参考资料 8.1 / 8.2

### 9.2 免责声明模板（必须出现在 F7）

> 本站内容由 AI 起草 + 律师人工复核，仅供学习参考，**不构成具体法律咨询**。  
> 您的具体问题请咨询执业律师，或拨打免费法律热线 **12348**。  
> 紧急人身安全事件请立即拨打 **110**。

### 9.3 内容生产流程（必须出现在 F7）

1. **选题**：从 V1 覆盖的 6 大情形里，按用户搜索量 / 微博热搜 / 朋友群转发频次排序选
2. **起草**：AI 根据法条原文 + 案例拟初稿（每句都能点开出处）
3. **复核**：人工核对法条编号、原文一字不差，行动步骤查常识
4. **发布**：标 `author_type` / `reviewed_by` / `updated_at`，永久可追溯

---

## 10. 时间表（Day 1 ~ Day 28）

| Day | 工作 | 产出 |
|-----|------|------|
| 1 | 项目环境与规则 | AGENTS.md |
| 2 | 仓库 + 占位页 + 忽略规则 | index.html / .gitignore |
| 3 | 需求研究 | research.md |
| **4** | **PRD 撰写（本文件）** | **PRD.md** |
| 5 | 技术选型 + 数据格式 | tech-stack.md / cards.json schema |
| 6 | UI 设计稿 | Figma / 草图 |
| **7** | **MVP 上线** | **线上首页 + 12 张卡片 + 7 个功能** |
| 8 ~ 14 | 内容填充（每周 12 张）| +120 张卡片 |
| 15 | 用户研究（10 个朋友访谈）| insights.md |
| 16 ~ 21 | V1.1 优化（搜索、收藏体验）| |
| 22 ~ 28 | V2 策划（账号 / 复杂推荐 / 付费的合规路径）| v2-plan.md |

---

*Day 4 输出 · Day 5 据此做技术选型 · Day 7 据此出 MVP*  
*所有字段 / 验收标准以本文件 v1.1 为准 · 2026-09-18*
