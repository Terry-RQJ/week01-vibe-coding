# AGENTS.md — 第 1 周·Day 1 项目规则

> 这份文件叫 **AGENTS.md**，是给 AI 协作者（也就是我）看的「项目宪法」。  
> 目的：每次新会话开始，先读这份文件，按里面的规则行动，不要乱跑。  
> 把它放在项目工作区文件夹的根目录，所有 Agent 都会自动加载。

---

## 1. 项目基本信息

- **项目名**：week01-vibe-coding（第 1 周·vibe coding 学习项目）
- **工作区路径**：`C:\Users\Administrator\Desktop\Vibe Coding\`
- **学习周期**：Day 1 ~ Day 7
- **今日**：Day 1——装环境 + 立规则
- **今日范围**：**不写代码、不建仓库**（留到 Day 2）

---

## 2. 我（AI 协作者）必须遵守的规则

1. **先读再动**：每次开新对话，先读 `AGENTS.md`，再决定做什么。
2. **今日不做**：写代码、建仓库、改业务逻辑——Day 1 只做环境与规则。
3. **降级优先**：卡住就先跳过、记成「待办」、明天问老师，不要硬刚。
4. **保护隐私**：不外传账号、密码、token；不上传敏感信息到公开仓库。
5. **能跑就别装新的**：能用现有工具完成的事，就别引入新依赖。
6. **每一步留痕**：关键操作（装环境、改配置、建文件夹）都要在日志里写下来。
7. **当天只做当天任务**：学员没说「进入下一板块」就不越界；不在一个回合内擅自推进板块（如发现今天该结束就停下问下一步）。
8. **法条带核验日期**：任何「法条 / 司法解释 / 案例」内容，必须在交付时带「上次核验日期」（字段名 `last_verified_at`，值格式 `YYYY-MM-DD`），不只是写在对话里。

---

## 3. 我们的协作约定

| 场景         | 我会怎么做                       |
| ---------- | --------------------------- |
| 你说「解释一下 X」 | 用零基础能懂的话，配图配例子，不堆术语         |
| 你说「装 X」    | 先看是否已装，再装；装完跑一条验证命令         |
| 你说「卡住了」    | 立刻停下，记进 `## 待办`，给两个最小可行的下一步 |
| 你给截图       | 我按图里的路径/界面内容判断，不要瞎猜         |
| 你交任务       | 我对照「完成标准」逐项打勾，不擅自加项         |
| 你要写给用户看的话 | 写给读者的话要会「打比方」：任何面向用户的段落超过 5 行没有举一个生活例子，我必须重写。 |
| 你要我改站内链接 | 全站所有 HTML 内部链接一律相对路径（`./css/...` 或 `../css/...`，**绝对不要** `/css/...`）。改任何 `<a href>` / `<link href>` / `<script src>` 之前我先看这一眼。 |

---

## 4. Day 1 完成标准（验收清单）

- [x] **环境**：WorkBuddy、Git、Node.js 三项就位（Git 2.51+，Node v22.22.2+）
- [x] **账号**：GitHub 账号已注册（你去 github.com 注册，告诉我用户名即可）
- [x] **工作区文件夹**：`week01-vibe-coding` 已建好
- [x] **规则文件**：本文件 `AGENTS.md` 已存进工作区根目录
- [x] **解释过关**：你能用自己的话讲清 vibe coding 和 skill

---

## 5. Day 1 待办（明早问老师）

- [ ] GitHub 账号注册 + 头像设置（自行去 github.com 完成）

---

## 6. 名词速查

- **vibe coding**：用「自然语言描述意图」让 AI 生成代码的方式，重点在讲清楚「我要什么」，不在手写每一行。
- **skill**：AI 的「操作手册」。一个 skill 就是一套专门的工作流，比如「怎么读 Excel」「怎么发邮件」。遇到专门任务就调相应 skill。
- **Agent**：能自主完成多步任务的 AI 协作者。今天的我就是你的 Agent。
- **工作区（workspace）**：你和 AI 共同操作的那块地盘。所有项目文件都放这里。

---

*本文件由 WorkBuddy 在 Day 1 协助生成；以后每次重要变更，先改这里，再动手。*
